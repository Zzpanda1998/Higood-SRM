import SimpleTablePage from "../SimpleTablePage";
import { purchaseOrders } from "../../mock/purchaseOrders";
export default function PurchaseOrderTracking() { return <SimpleTablePage title="采购订单跟踪" desc="用于展示内部确认、发货、在途、收货、质检、入库、对账节点状态。" rows={purchaseOrders.map((x) => ({ ...x, node: x.status, confirm: x.status === "已确认" ? "已确认" : "处理中", ship: x.shippedQty > 0 ? "已发货" : "未发货", transit: ["在途", "已发货"].includes(x.status) ? "运输中" : "-", receive: x.receivedQty > 0 ? "已收货" : "待收货", qc: x.status.includes("质检") ? "质检中" : "-", inbound: x.inboundQty > 0 ? "已入库" : "待入库", statement: x.inboundQty > 0 ? "可对账" : "-" }))} columns={[{ key: "orderNo", title: "采购订单号" }, { key: "supplier", title: "供应商" }, { key: "purchaseType", title: "采购类型" }, { key: "node", title: "当前节点" }, { key: "confirm", title: "确认状态" }, { key: "ship", title: "发货状态" }, { key: "transit", title: "在途状态" }, { key: "receive", title: "收货状态" }, { key: "qc", title: "质检状态" }, { key: "inbound", title: "入库状态" }, { key: "statement", title: "对账状态" }]} />; }
